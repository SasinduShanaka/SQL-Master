# SQL Master — Frontend

This is a minimal Vite + React scaffold. Install and run:

```bash
cd frontend
npm install
npm run dev
```

The frontend expects the backend API proxied at `/api/*` (configure your dev server or proxy accordingly).
